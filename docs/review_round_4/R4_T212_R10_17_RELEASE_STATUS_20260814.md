# R4 T212：r10.17 当前版本绑定与 DOI 预检状态

日期：2026-08-14  
状态：`CANDIDATE_NOT_DOI_ARCHIVED`

## 绑定内容

本轮 release 将 T209 Manchester v1.1 cohort reconciliation、T211 四代理终审、当前 README、`PROJECT_STATE.yaml` 和任务账本纳入同一版本链。计划 tag：`v0.1.3-r10.17`。

`v0.1.3-r10.16` 保持不可变，不再被称为包含 T209 v1.1 的当前版本。新 release manifest 的 `source_commit` 指向 release-metadata commit 之前的代码/文档源状态；manifest 自身排除自哈希，避免循环依赖。

## 内容边界

- 公开 release 只包含许可证允许的 tracked software、protocol、registry、receipt schema 和审计文档；
- PMC10257194 的 CC-BY-NC-ND raw workbook、source map 和 numeric derivatives 仍为 analysis-only；
- Manchester author matrix 因 repository license 不明确仍为 analysis-only；
- T209 的 v1.1 修正已绑定到本轮 release，而不是回写旧 tag；
- release 不声明 independent validation、protected lockbox、no-author reproduction、external adoption、DOI archive 或 `scientific_submission_ready`。

## DOI 状态

本地可完成：manifest、git tag、git archive、tarball、SHA-256 sidecar、GitHub release asset 和 clean-checkout verification。  
本地不能生成：Zenodo/等效服务的 DOI、archive locator 或第三方归档 receipt。

因此，在归档服务返回不可变记录之前必须保持：

```text
doi_archived = false
doi_status = PENDING_NOT_ARCHIVED
scientific_submission_ready = false
```

## 验收条件

1. `git rev-parse v0.1.3-r10.17^{}` 与 release assembly 一致；
2. manifest 文件列表与 tagged archive 的 tracked 内容一致；
3. tarball 和 SHA-256 sidecar 可复核；
4. README、Issue #2、KAUST checkout 和 release notes 指向同一 tag；
5. 所有 external gates 仍为 false，直到真实第三方/归档服务回执出现。

本报告不是 DOI receipt，也不是外部科学复现或采用证明。
